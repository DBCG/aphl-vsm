apiVersion: v1
kind: Service
metadata:
  name: cqf-ruler-vsm
  namespace: {{ .Values.namespace }}
spec:
  type: LoadBalancer
  selector:
    app: cqf-ruler-vsm
  ports:
    - name: http
      port: 80
      targetPort: 8080
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: cqf-ruler-vsm
  labels:
    app: cqf-ruler-vsm
  namespace: {{ .Values.namespace }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cqf-ruler-vsm
  template:
    metadata:
      labels:
        app: cqf-ruler-vsm
    spec:
      containers:
        - image: {{ .Values.aws.account_id }}.dkr.ecr.us-east-1.amazonaws.com/cqf-ruler:{{ .Values.tag }}
          imagePullPolicy: Always
          name: cqf-ruler-vsm
          resources:
            requests:
              memory: "7Gi"     
              cpu: "1500m"
            limits:
              memory: "7.5Gi"
              cpu: "1800m"
          env:
            - name: hapi.fhir.server_address
              value: {{.Values.protocol}}://{{ .Values.cqf_ruler.app_url}}/fhir
            - name: hapi.fhir.allow_multiple_delete
              value: "true"
            - name: hapi.fhir.fhir_version
              value: R4
            - name: hapi.fhir.cql.cql_logging_enabled
              value: "true"
            - name: spring.datasource.hikari.maximum-pool-size
              value: "30"
            - name: redis.host
              value: {{ .Values.vsm_app.redis_host_url}}
            - name: redis.port
              value: "6379"
            - name: redis.database
              value: "4"
            - name: _JAVA_OPTIONS
              value: '-Xmx7500m'
            - name: spring.datasource.url
              value: {{ .Values.cqf_ruler.db_url }}
            - name: spring.datasource.driverClassName
              value: org.postgresql.Driver
            - name: spring.datasource.username
              value: smile_cr
            - name: hapi.fhir.security.enabled
              value: "true"
            - name: hapi.fhir.logger.format
              value: >
                Path[$${servletPath}] Source[$${requestHeader.x-forwarded-for}]
                Trace[$${requestHeader.x-b3-traceid}]
                Operation[$${operationType} $${operationName} $${idOrResourceName}]
                UA[$${requestHeader.user-agent}] Params[$${requestParameters}]
                ResponseEncoding[$${responseEncodingNoDefault}]
            - name: hapi.fhir.reuse_cached_search_results_millis
              value: "-1"
            - name: hapi.fhir.security.basic_auth.enabled
              value: "true"
            - name: hapi.fhir.security.basic_auth.username
              value: admin
            - name: hapi.fhir.casereporting.enabled
              value: "true"
            - name: hapi.fhir.rulercr.enabled
              value: "true"
            - name: hapi.fhir.rulercr.vsac-username
              value: apikey
            - name: hapi.fhir.rulercr.vsac-api-key
              valueFrom:
                secretKeyRef:
                  name: vsm-app-secrets
                  key: vsac-api-key
            - name: hapi.fhir.security.basic_auth.password
              valueFrom:  
                secretKeyRef:
                  name: vsm-app-secrets
                  key: cqf-ruler-basic-auth-password
            - name: spring.datasource.password
              valueFrom:
                secretKeyRef:
                  name: prod-db-passwords
                  key: vsm-db-password
            - name: spring.jpa.properties.hibernate.dialect
              value: ca.uhn.fhir.jpa.model.dialect.HapiFhirPostgres94Dialect
          ports:
            - containerPort: 8080
              protocol: TCP
      tolerations:
      - key: "memory-intensive"
        operator: "Equal"
        value: "true"
        effect: "NoSchedule"
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
            - matchExpressions:
              - key: "kubernetes.io/memory-intensive"
                operator: In
                values:
                - "true"
