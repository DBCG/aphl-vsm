Setup

Install VS Code Plugin by following instructions in this video


Running Locally

The vsm app should be running and on production mode with `npm run build && npm run start`

`npx playwright test --headed `

This will allow you to view the test in a chromium window